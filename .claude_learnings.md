# Claude Learnings — Voicer

Read this file before debugging. Log root causes and fixes here to avoid repeating mistakes.

---

## Format
```
### [Date] — Short description
**Error:** What failed
**Root Cause:** Why it failed
**Fix:** What was changed
```

---

### 2026-06-10 (pm) — Reconnect engine v1 shot down its own handshakes

**Error:** After shipping the auto-reconnect engine: tap Connect → "Signaling" ~15s (normal) → "Connecting" forever. Host log showed `offer → answer → offer → answer...` with NO "ICE completed" ever — while pre-update sessions completed fine.

**Root Cause (two stacked, both mine):**
- The 12 s offer-answer watchdog was SHORTER than legitimate signaling. The phone waits up to 10 s for ICE gathering (TURN allocation, `setTimeout(resolve, 10_000)` safety in peer.ts) before writing the offer; the answer lands ~13-15 s after tap. So the watchdog fired mid-handshake on EVERY connect and the re-dial killed its own predecessor — an endless storm.
- The re-dial downgraded to STUN-only: `reconnect()` read ICE servers from the session row, but mid-handshake the row holds the offer/answer (not host-ready) → `undefined` → `FALLBACK_ICE`. This network demonstrably needs the Cloudflare relay → ICE hangs in "connecting" forever.

**Fix:** watchdog 12 s → 30 s (it exists only to catch a host that restarted mid-signaling); `iceServers` persisted in the session store and `connectToHost` resolves param → stored → undefined so re-dials NEVER lose TURN (CF creds ttl=86400); unique Realtime channel suffix per peer attempt (`peer-${seq}` — reusing one name across rapid close/resubscribe lets old cleanup kill the new sub); dashboard Connect resets the reconnect-attempt budget.

**Key Rule:** Before adding ANY timeout that triggers connection teardown/retry, measure the real end-to-end timing of the path it guards (here: 10 s gathering + answer RTT ≈ 15 s, so a 12 s guard = 100 % false-positive rate). And a reconnect path must carry forward every credential the original connect had — re-reading shared state that's mid-mutation (signaling row during handshake) silently loses them.

---

### 2026-06-10 — Voice dies minutes into a connection + reconnect dead-ends

**Error 1 (voice):** Dictation works after connect, then dies for the rest of the connection. voicer.log showed the chain on repeat: `GoAway received — server disconnecting in 50s` → `Gemini recv error: 1011 The service is currently unavailable` → `Flush failed 3 times — declaring session dead, forcing restart` → `Task exception was never retrieved: ConnectionClosedError` in `_start_voice` → endless `Gemini send error` spam.

**Root Cause (three stacked):**
- The Live API hard-closes every WebSocket at ~10 min (GoAway after pipeline flush #35 ≈ 8.75 min, consistently). Our GoAway handler only *logged* "will restart" — it broke the inner receive loop and waited for the 1011 death.
- `on_session_dead` was gated on `host._voice_active` — restarts only happened if the user was MID-DICTATION when the session died. Idle deaths (the common case) were never healed.
- `begin_turn()` on a dead WS raises `websockets.ConnectionClosedError`, which `_start_voice` only caught as `(RuntimeError, TimeoutError)` → unhandled task exception → `_voice_active` stuck True → zombie voice until full reconnect.

**Fix:** GoAway now fires `on_session_dead` immediately (restart with resumption handle, interim_buffer + audio queue preserved). `_restart_gemini` drops the `_voice_active` gate (restarts idle sessions; quiet on idle failure, error-status on mid-turn failure). `restart()` retries 3× and clears a stale resumption handle between attempts. `begin_turn` wraps transport errors into RuntimeError; `_start_voice` catches broad `Exception`, restarts, and RETRIES the turn so the tap survives; plus an outer catch-all so no `_start_voice` exception can ever go unretrieved again. Tests: `test_session_self_heal.py`.

**Error 2 (reconnect):** After disconnecting, the dashboard sometimes showed "Desktop host is offline" until the host was restarted. Log showed `_on_bye` dying on `httpx.ReadTimeout` inside `update_pc_status_async` — the host-ready republish on the next line never ran.

**Root Cause:** Load-bearing Supabase writes had no retry and shared one try-scope, so a single transient timeout skipped the republish and left the row stale forever (nothing ever rewrites it while idle). Phone side: after `connected` the PWA unsubscribes from signaling and its only "recovery" was `pc.restartIce()` — a dead-end (aiortc doesn't support ICE restarts, and there's no renegotiation channel anyway).

**Fix:** Host — `_patch_session_with_retry` (3×, 10s timeout) under both writes; `_on_bye`/`_on_connection_state` make each write independently best-effort; new `_host_ready_heartbeat` republishes every 30s while idle (skips while `_offer_lock` held or pc is new/connecting/connected). PWA — full auto-reconnect engine in use-session.ts: every recovery is a fresh re-dial (re-fetch session row by userId — host restarts create NEW rows — then new offer); triggers on visibilitychange/pageshow resume, ICE failed, 15s-disconnected, host-side close, and a 12s offer-answer watchdog; explicit Disconnect sets `_userDisconnected` to suppress it all. peer.ts detaches pc/dc handlers in close() so stale connections can't clobber fresh state. Dashboard treats `pc_status=connected` as connectable (host answers fresh offers anytime — a new offer instantly evicts a zombie pc). Tests: `test_signaling_resilience.py`.

**Error 3 (clipboard):** `Failed to register window class` on every connection after the first; clipboard-push silently dead for sessions 2+.

**Root Cause:** Win32 window classes live for the process lifetime. Re-registering per `start()` fails with ERROR_CLASS_ALREADY_EXISTS (treated as fatal). Worse, the class held a pointer to the FIRST run's WNDPROC ctypes thunk, which got GC'd when `_run` returned — the early-return bug was accidentally shielding a dangling-pointer crash.

**Fix:** Module-level `_WND_PROC_THUNK` (never GC'd) + register-once `_ensure_class_registered()` + `_active_watcher` indirection for dispatch.

**Key Rules:** Gemini Live sessions MUST self-heal independently of voice-turn state — treat GoAway as "restart now", not a warning. Any state-machine callback chain that gates recovery on user activity will fail for idle deaths. Supabase writes that unblock reconnection need retries AND a periodic self-heal loop — one lost write must never require a process restart. The ONLY phone-side recovery for a dead aiortc peer is a brand-new offer/answer cycle.

---

### 2026-04-17 (late pm) — Session-lifecycle promotion: pre-warm Gemini Live at connect-time

**Error (UX, not a crash):** Every voice-tap paid ~7 s of first-word latency. Measured: ~4.1 s to first audio (token fetch + WS handshake + session setup), ~7.1 s to first transcription (+ model TTF). Monday app launch needs <1 s steady-state.

**Root Cause:** `GeminiLive` was created in `_start_voice` on every voice-tap and destroyed in `_stop_voice`. The Gemini WebSocket handshake + ephemeral token fetch ran serially on the critical path of every tap. The Live API supports long-running sessions with manual activity cycles; we just weren't using them.

**Fix (architectural — promoted lifecycle from per-tap to per-connection):**
- **Host [host/gemini_live.py](host/gemini_live.py):** New `begin_turn()` resets per-turn state (interim_buffer, TTF timestamps, RMS window, peak qsize, filter rejections, start_ts) and cycles `activity_end/start` (skips end/start on turn #1 because `start()` already sent the initial `activity_start`). `_restart_done_event` (set at init, cleared by `restart()` at entry, set in `finally`) blocks `begin_turn` on in-flight 29-min token refreshes so a voice-tap doesn't pile on a second restart. `stop()`, `flush_final()`, `_send_loop`, `_recv_loop`, `_flush_loop`, `_token_expiry_loop` unchanged — all were already session-lifetime-correct.
- **Host [host/webrtc_host.py](host/webrtc_host.py):** New `_prewarm_gemini()` spawned at top of `_handle_offer` (gated by `GEMINI_PREWARM` env, default true). Runs parallel with ICE gathering (saves 1–2 s). `_teardown_gemini()` is the single death point — called from DC `on_close`, `_on_bye`, `_on_connection_state("failed"|"closed")`. `_start_voice` now awaits `_gemini_ready` (8 s timeout → `reason=handshake`) then calls `begin_turn()` (2 s timeout → `_restart_gemini`). `_stop_voice` stops touching `_gemini` entirely — session survives across turns. `_send_status` hardened to queue on `channel is None` or `readyState != "open"` so pre-warm errors fired before DC opens still reach the PWA via `on_open` drain.
- **PWA [src/hooks/use-voice.ts](src/hooks/use-voice.ts) + [src/hooks/use-session.ts](src/hooks/use-session.ts):** Module-level `prewarmAudio()` runs in `connectToHost` action — creates `new AudioContext({sampleRate:16000})` + `audioWorklet.addModule()`. **Does NOT call `getUserMedia`.** Warm-path `startListening` resumes a suspended ctx (mobile backgrounding) and rebinds `worklet.port.onmessage` every call (closure captures DC ref — stale after reconnect). Pre-warm state lives at module level, not hook refs, so the zustand action can access it.

**Two closure bugs bit me writing the tests (Python-specific, not product bugs — worth documenting):**
1. `for state in (...)` loops where tracker closures close over loop locals: the `original_stop` and `stop_calls` free vars share ONE scope cell that gets rebound each iteration. Iter 2's `tracked_stop` calls `original_stop` which is now iter 1's `tracked_stop` — and because iter 1's closure also resolves `original_stop` from the same cell (now pointing to itself), infinite recursion. Fix: pytest.parametrize each state into its own test — separate function scopes, no shared cell.
2. `_teardown_gemini`'s early-return path must NOT null `_gemini_ready`. Teardown is called at the TOP of `_prewarm_gemini` for idempotency (handles offer-arrives-twice / ICE re-answer). If `_gemini_ready` was just allocated by the caller and teardown nulls it, the `.set()` in prewarm's `finally` no-ops and `_start_voice`'s `wait_for` times out at 8 s. Belt-and-suspenders: `_prewarm_gemini` captures `ready_event = self._gemini_ready` locally at entry and signals the local, not the instance attribute.

**Key Rules:**
- **Product decision — `getUserMedia` stays on voice-start, NOT connect.** Pre-warming the mic would leave the OS mic indicator on for the entire connected-but-idle session (hours for pocket-mode users). Costs ~50–300 ms on tap #1, but preserves the UX contract "mic lights = I'm being listened to." Post-Monday, if tap #1 latency matters more than indicator cleanliness, revisit as an opt-in speed-mode.
- **Pre-warm is indicator-free** — `new AudioContext()` + `audioWorklet.addModule()` only. The mic indicator is lit exclusively by `getUserMedia`.
- **Rollback flags** — `GEMINI_PREWARM=false` (host) and `NEXT_PUBLIC_AUDIO_PREWARM=false` (PWA) fall back to legacy cold-start. Keep both + legacy branches for a full release cycle before removing.
- **Idle hosted-mode API cost** — a connected-but-idle phone burns ~1 audio-input token per 15 s flush cycle + periodic token re-auth every 29 min. Fractions of a cent per idle-hour per user. Flagged for first billing review; if it becomes material, gate `_flush_loop` behind "any turn in the last N seconds."

**Verification:** 50 host pytest tests pass (2 real-API skipped). 44 node tests pass. End-to-end phone test pending — expect tap #2+ <500 ms steady-state.

---

### 2026-04-17 (pm) — Post-install regression after shipping the 12-fix hardening

**Error:** After installing the new .exe, PWA showed "Voice model unavailable — update the app" on every dictation attempt. Separately, the host crashed on startup for the first few launches with Supabase 400 errors.

**Root Causes (three stacked):**
- **Installer `.env` had `USER_ID=smoketest`** — a test-value leak, not a valid UUID. Supabase `sessions` / `profiles` queries 400 on non-UUID casts, `upsert_session` raised, uvicorn lifespan failed → `host-offline` on phone. Installer onboarding should validate UUID format before writing.
- **Google retired `gemini-2.5-flash-preview-native-audio-dialog`** server-side around this date when consolidating `v1alpha` into `v1main`. Old pin returns 1008 "not found for API version v1main, not supported for bidiGenerateContent". My pinned-model guard + `reason="model"` classifier caught it cleanly — PWA showed the right friendly copy instead of failing silently. That's exactly what the guard was built for.
- **`load_dotenv()` ordering**: `webrtc_host.py` imports `gemini_live` (line 35) BEFORE `supabase_client` (line 37, which runs `load_dotenv()`). So `MODEL = os.environ.get("GEMINI_MODEL", ...)` at `gemini_live.py` top evaluated BEFORE `.env` was loaded — the env-override escape hatch was silently ignored. Fix: `gemini_live.py` calls `load_dotenv()` itself at module top.
- **Google's prepaid-billing enforcement from 2026-03-23**: after fixing the model name, the next 1011 error was "prepayment credits depleted". Live API native audio is paid-only (~$0.27/hr at $3 per 1M audio-input tokens, 25 tokens/sec). Resolved by topping up AI Studio credits; flagged as follow-up to add a `quota`/`billing` reason to the error taxonomy so PWA shows a useful message instead of the generic "Voice session failed — try again" for 1011s.

**Fix:**
- Installed host `.env`: USER_ID swapped to real UUID.
- `host/gemini_live.py`: `from dotenv import load_dotenv; load_dotenv()` at module top; default MODEL → `gemini-2.5-flash-native-audio-preview-12-2025`.
- `host/tests/test_model_pinned.py`: `ALLOWED_MODELS` updated with new pin + retirement note.
- `memory/project_audio_pipeline.md`: updated pinned-model reference + added the load_dotenv-ordering invariant.

**Key Rule:** Any module that reads `os.environ.get(...)` at import time MUST call `load_dotenv()` itself — do not rely on some other module loading first. Ordering is fragile, failure is silent. Also: Google model-name rotations are now roughly quarterly. The `-preview-…-MM-YYYY` date-stamped form is safer than undated preview names — treat any `-preview` suffix without a date as an alias that will rotate.

**Latency follow-up (not yet fixed):** live test shows first-word → on-screen latency ~3-4 s. Target is 300-800 ms. Suspect: new model's first-chunk TTF, TURN relay fallback vs P2P, or Gemini v1main rollout perf. Logged for later.

---

### 2026-04-17 — Voice input "nothing at all" on Pixel 7 Pro + first-words drop

**Error:** Two symptoms at once — (1) first 3-5 s of audio missing for several days, (2) today nothing transcribed at all on Pixel 7 Pro.

**Root Cause (12 overlapping issues, documented as Causes A–L in the plan):**
- **F (today's primary)**: `new AudioContext({ sampleRate: 16000 })` is silently ignored on Android Chrome — context runs at 48 kHz native rate. Worklet emitted 1600 samples of 48 kHz data labelled `audio/pcm;rate=16000`; Gemini interpreted it as 16 kHz → audio plays ~3× too fast → empty transcript. No client-side error.
- **A (first-3-5-seconds drop)**: `self._active = True` was set only after the Gemini token fetch (~1 s) + WS handshake (~0.5 s) completed. Audio chunks arriving during boot hit the `if not self._active: return` guard and were silently dropped.
- **B**: `_flush_loop` did `activity_end → sleep(50ms) → activity_start`. Audio sent during that gap was discarded server-side (disabled-VAD mode).
- **C**: `_stop_voice` didn't wait for the tail transcription — last 200-500 ms of audio's transcription never arrived.
- **D**: `_start_voice` was an untrapped `loop.create_task` — any failure (bad token, rotated model, WS fail) became a logged task error the PWA never heard about; PWA stayed stuck on "listening".
- **E**: `MODEL = "gemini-2.5-flash-native-audio-latest"` was a floating alias. Google rotates these silently.

**Fix (shipped 2026-04-17):** 12-step hardening pass, one commit. See [memory/project_audio_pipeline.md](C:/Users/Korisnik/.claude/projects/c--Antigravity-Voicer/memory/project_audio_pipeline.md) for the full map. Highlights:
- Worklet reads `processorOptions.inputSampleRate`, anti-aliases with 7-tap windowed-sinc FIR low-pass at ~7.5 kHz, decimates to 16 kHz with fractional-sample `_phase` carryover for non-integer ratios (44.1k→16k). Bypass path at 16 kHz for iOS Safari.
- `_active = True` flipped as first line of `GeminiLive.start()`, before any await.
- `asyncio.Lock` brackets `activity_end/start` pair in `_flush_loop` and `send_realtime_input` in `_send_loop`. Sleep removed.
- `flush_final(timeout=1.5)` drains queue, waits on an `asyncio.Event` signaled by `_recv_loop` on next `turn_complete` or `input_transcription`. Short-circuits if `_turn_active` already False (no deadlock).
- Error taxonomy: `token` / `model` / `handshake` / `no-audio` / `unknown`. `_send_status` queues to `_pending_status_flushes` when channel not open. PWA shows `friendlyMessageFor(reason)` copy.
- `MODEL = "gemini-2.5-flash-preview-native-audio-dialog"` pinned explicitly; env override available; `ALLOWED_MODELS` allowlist enforced in test.
- 3 s startup + 10 s mid-session audio watchdogs. Track-mute rebuild. start/stop race via `_voice_starting_task`. Rolling-5s RMS warning at < 0.0002 (accounting for `noiseSuppression: true`).
- Test coverage: 31 host unit tests + 4 PWA sample-rate + 4 rapid-toggle + 11 error-status + 1 real-API integration (parameterized BYOK/hosted, opt-in via `RUN_REAL_API_TESTS=1`). Extracted `WORKLET_CODE` + `friendlyMessageFor` to `src/hooks/voice-worklet.ts` so `.mjs` tests can import without React.

**Key Rule:** NEVER trust `AudioContext.sampleRate` to match what was requested — always read it back and route through the decimation worklet if it doesn't match 16 kHz. NEVER set an `_active` flag after any `await` in a session-start path. The `flush_final()` path is the only supported voice-stop — do not bypass it with inline `interim_buffer.strip()` reads.

### 2026-03-26 — Trackpad mode taps landing at wrong position

**Error:** In trackpad/mouse mode, tapping on the phone screen clicked at the wrong position on the PC — off by a wide margin.

**Root Cause:** Trackpad taps sent `{ type: "click" }` which fires `pyautogui.click()` at the invisible cursor's current position (wherever relative mouse movements accumulated to). But the user's mental model is "I see the screen, I tap on a button, it should click there" — same as voice mode. The cursor position was completely disconnected from where the user tapped.

**Fix:** Changed trackpad taps to use absolute coordinates via `getNormalizedCoords(touch.clientX, touch.clientY)` — same coordinate math as voice mode taps. Sends `{ type: "tap", x, y }` with normalized 0.0-1.0 coords. Also syncs `pcCursorPos` to the tapped position so relative drags continue from the right spot.

**Key Rule:** Any tap-to-click gesture (voice or trackpad) MUST use `getNormalizedCoords()` with `videoRef.current.getBoundingClientRect()` — never `window.innerWidth`. The video uses `object-contain` which letterboxes, and may be zoomed via CSS transform. `getBoundingClientRect()` accounts for both.

### 2026-03-26 — Coordinate math for zoomed video taps

**Error:** Potential for taps to land wrong when video is CSS-zoomed.

**Root Cause:** `getBoundingClientRect()` returns the visual (scaled) rect of the video element. When zoomed 2x, the rect is 2x the viewport. `getNormalizedCoords` computes `(clientX - rect.left) / rect.width` which correctly maps to the visible portion of the video — e.g., tapping at center of screen while 2x zoomed on center = 0.5 normalized = center of PC screen.

**Fix:** No fix needed — `getNormalizedCoords` already handles zoom correctly because CSS transforms affect `getBoundingClientRect()`. This is why we must always use the video element rect, never `window.innerWidth`.
