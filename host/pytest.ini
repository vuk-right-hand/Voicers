[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
markers =
    real_api: hits the real Gemini Live API; opt-in via RUN_REAL_API_TESTS=1
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
